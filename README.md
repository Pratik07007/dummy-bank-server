# Add your psql string in root level .env file
# run npm i
# migrate the schema ==> npx prisma migrate dev --name <migration name>
# generate client ==> npx prsima generate
# npm run dev to run the project locally 
